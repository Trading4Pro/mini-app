# Copy to .env on the VPS and fill in. .env is git-ignored (never committed).
# This is the @BotFather token; it overrides botToken in config.json.
BOT_TOKEN=123456:your-telegram-bot-token
# INFO for normal use; DEBUG to print chat ids you can copy into config.json.
LOG_LEVEL=INFO
