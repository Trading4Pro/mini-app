# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
dependency-reduced-pom.xml

# Real config holds the bot token — keep it out of git. Commit config.example.json instead.
config.json

# Environment / secrets
.env
.env.*
!.env.example
*.local

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/
